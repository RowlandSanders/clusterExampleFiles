apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: streaming-platform
spec:
  selector:
    app: analytics-service
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP