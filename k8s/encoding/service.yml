apiVersion: v1
kind: Service
metadata:
  name: encoding-service
  namespace: streaming-platform
spec:
  selector:
    app: encoding-service
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP